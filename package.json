{
  "name": "proj_0814",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:admin": "npm run dev --prefix admin",
    "dev:web": "npm run dev --prefix web",
    "dev:backend": "python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload --app-dir backend",
    "test": "python test/run_all.py",
    "test:backend": "python -m pytest test/backend -v"
  }
}
